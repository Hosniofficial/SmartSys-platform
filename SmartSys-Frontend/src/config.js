// src/config.js
// Best practice: Direct API calls to backend
// Development: Backend runs on localhost:8000 (php -S localhost:8000 -t public/)
// Frontend uses CORS to communicate directly
export const API_BASE = 'http://localhost:8000';
